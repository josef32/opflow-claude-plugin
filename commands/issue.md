---
description: Triage open issues from the orchestrator and scope them into plans.
---

Use the `issue` skill: list open (`status: created`) issues, group related ones, and either attach to an existing plan via `issue-attach` or create a new plan with `plan-create({ issueIds })`.
