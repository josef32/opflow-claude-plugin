---
description: Create or read documentation in the orchestrator's docs collection.
---

Use the `documentation` skill. For reads: `docs-discover` then `docs-get`. For writes: read the relevant code first, then `docs-write` with title, content, package, tags, and either `path` or `relatedEndpoint`.
