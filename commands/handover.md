---
description: Create a handover record for the work just completed (auto-detects commit + files, then asks for a summary).
---

Use the `handover` skill: create a handover record for the most recent commit. Auto-detect commit ID and changed files via git, then walk through the create-handover workflow.
