---
description: Fill in the project's Technologies field and the bootstrap.md doc by surveying the codebase. Run once per project (re-runnable, but unnecessary).
---

Use the `bootstrap` skill: read the project via `project-get`, survey the codebase, then call `project-update` (technologies) and `docs-write` (bootstrap.md).
