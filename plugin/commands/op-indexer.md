---
description: Re-sync this project's code to the orchestrator so `opflow search`/`context`/`files` reflect the latest source.
---

Use the `op-indexer` skill: run `opflow index` from the project root.
