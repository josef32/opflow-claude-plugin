---
description: Re-sync this project's code to the orchestrator so MCP search/context/files reflect the latest source.
---

Use the `op-indexer` skill: run `op-indexer -y` from the project root. Read `orchestration.json` first if you're not sure the project is set up; if it's missing, fall back to interactive mode (`op-indexer`).
