---
description: Create or update a project plan in the orchestrator (milestones, status, attached issues).
---

Use the `plan` skill: ask the user for plan details (name, affected packages, milestones, dependencies), generate the notes block, and call `plan-create`. If updating an existing plan, use `plan-update`.
